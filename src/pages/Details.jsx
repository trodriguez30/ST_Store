import React from "react";

export default function Details() {
  return <h1>Details</h1>;

  /*
  return (
    <div id="detail">
      <h1>{product.name}</h1>
      <p>{product.description}</p>
      <p>{product.price}</p>
      <img src={`/images/${product.image}`} alt={product.categgory}/>
    </div>
  )
  */
}
